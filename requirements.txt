# Core (robot voice + shared DSP core -- everything hand-written on top of these)
numpy>=1.23
flask>=2.2

# Singing voice (Aria).  The neural voice and the WORLD vocoder are what make it
# sound like a person; without them daftdsp.voice falls back to espeak.
piper-tts>=1.3        # neural (VITS) TTS
onnxruntime>=1.16     # runs the voice model
onnx>=1.15            # enables Piper's phoneme alignments
pyworld>=0.3          # WORLD vocoder: f0 / spectral envelope / aperiodicity
